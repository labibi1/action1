name: my first workflow

on: push

jobs:
    fisrt-job:
        run-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "my first github action Job"

        - name: list files
          run: ls

        - name: read file
          run: cat README.MD
